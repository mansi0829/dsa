#include <bits/stdc++.h>
using namespace std;

void mansi(int number)
{
    int arr[number], power = 0, ans;
    for (int i = 0; i < number; i++)
    {
        cin >> arr[i];
    }

    for (int k = 0; k < number; ++k)
    {
        
    }

    cout << ans << endl;
}

int main()
{
    // your code goes here
    int testcases, number;
    cin >> testcases;
    while (testcases--)
    {
        cin >> number;
        mansi(number);
    }
    return 0;
}
