#include <bits/stdc++.h>
#include <cctype>
#include <iostream>
#include <cstring>
#define ll long long int;
using namespace std;

int main()
{
    long long int t, n, k, b, s;
    
    cin >> t;
    while (t--)
    {
        cin >> n >> k >> b >> s;
    }
    return 0;
}